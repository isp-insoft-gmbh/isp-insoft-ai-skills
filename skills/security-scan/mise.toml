# security-scan - pure markdown. Build is a prune-copy into dist/.
[tasks.build]
description = "Pack SKILL.md and references -> dist/security-scan"
run = "node ../../scripts/pack.mjs"

[tasks.verify]
description = "Validate SKILL.md frontmatter"
run = "node ../../scripts/check.mjs"
